{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ConfGral</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Precioenvio</th>
                <td>{{ entity.precioEnvio }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('confgral') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('confgral_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
