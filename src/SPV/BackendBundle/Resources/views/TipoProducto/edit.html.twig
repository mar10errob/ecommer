{% extends '::admin.html.twig' %}

{% block contenido %}
    <h1>Editar Tipo de Producto</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('tipoProducto') }}">
            Regresar a la Lista
        </a>
    </li>
    {#<li>{{ form(delete_form) }}</li>#}
</ul>
{% endblock %}
