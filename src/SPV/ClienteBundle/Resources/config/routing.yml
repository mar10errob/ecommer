carrito:
    pattern: /carrito
    defaults: { _controller: ClienteBundle:Default:carrito }

pedido:
    pattern: /pedido
    defaults: { _controller: ClienteBundle:Default:pedido }

perfil:
    pattern: /perfil
    defaults: { _controller: ClienteBundle:Default:perfil }

cliente_login:
    pattern:  /login
    defaults: { _controller: ClienteBundle:Default:login }

cliente_login_check:
    pattern: /login_check

cliente_logout:
    pattern: /logout

cliente_regsitro:
    pattern: /registrate
    defaults: { _controller: ClienteBundle:Default:resgistro }

add_carrito:
    pattern: /add-carrito/{producto}/{cantidad}
    defaults: { _controller: ClienteBundle:Default:addcarrito, cantidad: 1}

remove_carrito:
    pattern: /remove-carrito/{producto}
    defaults: { _controller: ClienteBundle:Default:rmcarrito }