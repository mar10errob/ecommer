{% extends '::admin.html.twig' %}

{% block contenido -%}
    <h1>Editar Fecha Agendada</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('fechapedidos') }}">
			Regresar a la Lista
		</a>
    </li>
</ul>
{% endblock %}
